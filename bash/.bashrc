set -o vi

alias cp="cp -iv"
alias grep="grep --color=auto"
alias ls="ls --almost-all --human-readable --color=auto --format=long"
alias mkdir="mkdir -pv"
alias mv="mv -iv"
alias rm="rm -v"
alias tmux="tmux -f $XDG_CONFIG_HOME/tmux/tmux.conf"
